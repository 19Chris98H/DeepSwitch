export class ColorMap {
    constructor(points, colors) {
        this.points = points;
        this.colors = colors;
    }

    getPoints() {
        return this.points;
    }

    getColors() {
        return this.colors;
    }
}